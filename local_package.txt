clangd "https://github.com/clangd/clangd/releases"
pyright "npm install --prefix <localtion_pyright_will_be_installed> -g pyright" (install nodejs first: sudo apt install -y nodejs npm (on ubuntu forexample))
npm-global/bash-language-server "npm install --prefix <localtion_bashls_will_be_installed> bash-language-server" (run "npm init -y" first at the --prefix directory)
eclipse/jdt-language-server "https://download.eclipse.org/jdtls/milestones/1.41.0/"
ag "search engine for telescope. Need to install ag first"
https://www.nerdfonts.com/font-downloads .Then copy and unzip inside ~/.local/share/fonts. Then run fc-cache-64 -fv to install fonts
Need to install nerd font when using nvim-web-devicons plugin
Install lazygit: https://github.com/jesseduffield/lazygit/releases and add PATH to lazygit binary file
Install and configure neovim-remote: https://github.com/mhinz/neovim-remote/blob/master/INSTALLATION.md (should use pip3 install --user neovim-remote)


NOTE: On window
For jdtls, using jdtls.bat file instead of jdtls. Because jdtls.bat is using python so python needs to be installed ahead and add python to PATH. If you don't want to add python to PATH, you can open jdtls.bat and specify python with full path inside.

